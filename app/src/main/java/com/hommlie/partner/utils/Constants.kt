package com.hommlie.partner.utils

import com.hommlie.partner.utils.CommonMethods.dpToPx

object Constants {

    val DEFAULT_TEXT_SIZE = dpToPx(14).toFloat()
    val DEFAULT_SWIPE_DISTANCE = 0.85f
    val BTN_INIT_RADIUS = dpToPx(2).toFloat()
    val BTN_MORPHED_RADIUS = dpToPx(100).toFloat()
    val MORPH_ANIM_DURATION = 500
}